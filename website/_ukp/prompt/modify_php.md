# 업로드한 HTML 코드에 PHP 코드 추가(dh) (2026.02.09)
## 프롬프트
- 
## PHP 변수 리스트 (변수명 = 변수 설명)
```php
```
## 규칙
- ``` <?= ```, ``` <? ``` 사용하면 안되고 ``` <?php ``` 만 사용해줘.
- 데이터베이스 테이블 정보를 가지고 있는 변수는 업로드한 upload_db_select_sql.md, upload_db_table_ddl.md 파일 참고해줘.
- 업로드한 sql, json 파일은 파일명이 테이블명이야.
- db 테이블 쿼리결과 변수는 여러줄 인경우 ```array(array("컬럼" => "값", "컬럼" => "값"...)...)``` 과 같은 형태이고 1줄 인경우 ```array("컬럼" => "값", "컬럼" => "값"...)``` 와 같은 형태이다.
- 이미 php 코드가 작성되어있는경우 삭제하고 내가 요청한대로 php 코드를 작성한다.
- 조건에 따라 태그의 속성 또는 속성값을 출력해야하는경우 if 문 블록 대신 3항 연산자를 사용해줘.
- HTML 태그 전체를 echo로 출력하지 말고, PHP 태그를 닫고(?>) 순수 HTML로 작성해줘. 데이터 출력이나 로직이 필요한 부분에만 <?php ... ?>를 열어서 작성해줘.
- DB 쿼리 결과 변수는 항상 초기화되어 있다고 가정한다.
  - 1줄 결과: 데이터가 없더라도 쿼리의 모든 컬럼명이 키(key)로 존재하며 값은 null로 세팅된다. **데이터 존재 여부는 기본키(PK) 컬럼 값이 null인지 확인하여 판단해줘.** (키 존재 여부를 확인하는 isset()은 사용 금지)
  - 여러 줄 결과: 데이터가 없으면 빈 배열(```array()```)임이 보장된다. **데이터 존재 여부는 결과값이 빈 배열인지 확인하여 판단해줘.** (is_array()나 isset() 검사 없이 바로 foreach문 사용 가능)
